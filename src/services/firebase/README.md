# Firebase (Phase 3)

This folder is intentionally a placeholder in **Phase 1**.

Rules:
- No Firebase initialization in Phase 1.
- No env vars invented.
- When Phase 3 begins, we will add `firebase.ts` (client) and `admin.ts` (server) with explicit environment requirements.
